document.addEventListener('DOMContentLoaded',()=>{const typingArea=document.getElementById('switcher');const words=['community','nature','yourself'];let index=0;function startTyping(){if(index===words.length){index=0}const word=words[index];let i=0;const typingInterval=setInterval(()=>{if(i<word.length){typingArea.textContent+=word[i];i+=1}else{clearInterval(typingInterval);setTimeout(()=>{const eraseInterval=setInterval(()=>{if(typingArea.textContent.length>0){typingArea.textContent=typingArea.textContent.slice(0,-1)}else{clearInterval(eraseInterval);index+=1;startTyping()}},100)},3000)}},100)}startTyping();const scrollLink=document.getElementById('scroll-to-info');const targetElement=document.getElementById('info');scrollLink.addEventListener('click',function(e){e.preventDefault();targetElement.scrollIntoView({behavior:'smooth'})})});
